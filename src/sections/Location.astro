---
import { siteConfig } from "@/config/site";
---

<!-- TODO: Replace placeholder image and address with real Face Art Clinic location info. -->
<section class="section-pad bg-shell">
  <div class="container-page grid items-center gap-10 lg:grid-cols-2">
    <div class="h-80 overflow-hidden rounded-2xl bg-cream shadow-soft">
      <img
        class="h-full w-full object-cover"
        src="/images/breadcrumb-bg.jpg"
        alt={`Zona de ubicaciÃ³n de Face Art Clinic en ${siteConfig.city}`}
        loading="lazy"
      />
    </div>
    <div>
      <p class="eyebrow">UbicaciÃ³n</p>
      <h2 class="mt-3 font-display text-3xl font-semibold text-ink sm:text-4xl">
        VisÃ­tanos en {siteConfig.city}.
      </h2>
      <p class="mt-4 text-sm leading-7 text-cocoa">
        Face Art Clinic estÃ¡ ubicada en PanamÃ¡. ContÃ¡ctanos para recibir la direcciÃ³n exacta, disponibilidad de citas y detalles de acceso.
      </p>
      <div class="mt-6 space-y-3 rounded-2xl border border-borderSoft bg-ivory p-5 text-sm">
        <!-- TODO: Replace with confirmed address. -->
        <p class="font-semibold text-ink">{siteConfig.address}</p>
        {siteConfig.hours.weekdays && (
          <p class="text-cocoa">{siteConfig.hours.weekdays}</p>
        )}
        {siteConfig.hours.saturday && (
          <p class="text-cocoa">{siteConfig.hours.saturday}</p>
        )}
        <p class="text-cocoa">{siteConfig.hours.sunday}</p>
        <p class="text-cocoa/70">{siteConfig.hours.appointmentNote}</p>
      </div>
    </div>
  </div>
</section>
